Для построения проекта нужно открыть терминал в  папке
проекта и создать виртуальное окружение:

python -m venv venv

затем нужно его активировать и установить фреймворк

source venv/bin/activate
pip install flask

для запуска используется команда:

source venv/bin/activate
python main.py
